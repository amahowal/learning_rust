pub trait Messenger {
    fn send(&self, msg: &str);
}

pub struct LimitTracker<'a, T: Messenger> {
    messenger: &'a T,
    value: usize,
    max: usize,
}

impl<'a, T> LimitTracker<'a, T>
where
    T: Messenger,
{
    pub fn new(messenger: &'a T, max: usize) -> LimitTracker<'a, T> {
        LimitTracker {
            messenger,
            value: 0,
            max,
        }
    }

    pub fn set_value(&mut self, value: usize) {
        self.value = value;

        let percentage_of_max = self.value as f64 / self.max as f64;

        if percentage_of_max >= 1.0 {
            self.messenger.send("Error: You are over your quota!");
        } else if percentage_of_max >= 0.9 {
            self.messenger
                .send("Urgent warning: You've used up over 90% of your quota!");
        } else if percentage_of_max >= 0.75 {
            self.messenger
                .send("Warning: You've used up over 75% of your quota!");
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::cell::RefCell;

    struct MockMessenger {
        sent_messages: RefCell<Vec<String>>,
    }

    impl MockMessenger {
        fn new() -> MockMessenger {
            MockMessenger {
                sent_messages: RefCell::new(vec![]),
            }
        }
    }

    impl Messenger for MockMessenger {
        fn send(&self, message: &str) {
            // borrow_mut for RefCell CANNOT have more than one reference in scope at any time
            self.sent_messages.borrow_mut().push(String::from(message));
        }
    }

    #[test]
    fn it_sends_an_over_75_percent_warning_message() {
        let mock_messenger = MockMessenger::new();
        let mut limit_tracker = LimitTracker::new(&mock_messenger, 100);

        limit_tracker.set_value(80);

        assert_eq!(mock_messenger.sent_messages.borrow().len(), 1);
    }
}

/*
chat GPT is so good for explaining this stuff

here's an example it generated

struct ConsoleMessenger;

impl Messenger for ConsoleMessenger {
    fn send(&self, msg: &str) {
        println!("{}", msg);
    }
}

fn main() {
    // Create an instance of ConsoleMessenger
    let console_messenger = ConsoleMessenger;

    // Create a LimitTracker instance using ConsoleMessenger
    let mut tracker = LimitTracker::new(&console_messenger, 100);

    // Simulate value changes and check for messages sent
    tracker.set_value(80);
    // Output: Warning: You've used up over 75% of your quota!

    tracker.set_value(95);
    // Output: Urgent warning: You've used up over 90% of your quota!

    tracker.set_value(110);
    // Output: Error: You are over your quota!
}
*/

